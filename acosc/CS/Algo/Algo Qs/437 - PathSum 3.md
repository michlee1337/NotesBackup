optimal substructure requires: 
- [[num subsubs k == num interval differences k]]
- [[algo tricks]], [[caching]]
	- ex: binary tree
		- if only applicable along this path
		- update cache, calc subproblems, then reverse cache